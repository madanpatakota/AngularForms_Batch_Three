import { FirstamEmailDirective } from './firstam-email.directive';

describe('FirstamEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstamEmailDirective();
    expect(directive).toBeTruthy();
  });
});
